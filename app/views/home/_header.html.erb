<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <%= link_to 'Card Rack', root_path, class: "navbar-brand"%>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to 'About', home_about_path, class: "nav-link"%>
        </li>
        <% if user_signed_in? %>
            <li class="nav-item">
              <%= link_to 'Add Contact', new_contact_path, class: "nav-link"%>
            </li>
            <li class="nav-item">
              <%= link_to 'Contacts', contacts_path, class: "nav-link"%>
            </li>
            <li class="nav-item">
              <%= link_to 'Edit Profile', edit_user_registration_path, class: "nav-link" %>
            </li>
        <% end %>
        </ul>
        <form class="d-flex">
            <% if user_signed_in? %>
                    <%= link_to 'Sign Out', destroy_user_session_path,  data: { turbo_method: :delete }, class: "btn btn-outline-success me-2" %>
            <% else %>
                    <%= link_to 'Sign In', new_user_session_path, class: "btn btn-outline-success me-2" %>
                    <%= link_to 'Sign Up', new_user_registration_path, class: "btn btn-outline-success me-2" %>
            <% end %>
          </form>
    </div>
  </div>
</nav>